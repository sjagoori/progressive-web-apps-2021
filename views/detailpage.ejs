<!DOCTYPE html>
<html lang="en">

<%- include('partials/head', { title: "Detailpage" , css: "main" })%>

  <body>
    <a href="/">Home</a>
    <h1><%= title %></h1>
    <% if(data.length <=0){ %>
      <h1>Not found</h1>
      <% } else{ %>
        <div>
          <h1></h1>
          <ul>
            <% for (var i=0; i < 5; i++) { %>
              <li>
                <a href="https://www.reddit.com<%=data[i].data.permalink %>""><%= data[i].data.title %></a>
                
                <img src="<% data[i].data.preview.images[0].url %>" alt="" srcset="">

              </li>
          <% } %>
        </ul>
      </div>

    <% } %>
  </body>

</html>